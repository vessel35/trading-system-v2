{
  "_note": "This DESIGN harness needs NO database MCP — Phase A/B never connect to a live DB; they read init-scripts and legacy config as source files and produce the schema *design*, not executed DDL. The only server is codex-cli, an OPTIONAL cross-model (GPT-5.x via Codex) DESIGN critique used by the cross-model-reviewer node on the two heaviest Phase B stages (b-corelib, b-engine-eval). If OPENAI_API_KEY is unset, that node is skipped and the two in-repo gates (spec-consistency-auditor + cto-reviewer) still run. If a codex-cli MCP already exists globally, remove this declaration and use that one. Package version pinned (perf-audit 2026-06-05 P005).",
  "mcpServers": {
    "codex-cli": {
      "command": "npx",
      "args": ["-y", "codex-mcp-server@1.4.10"],
      "env": { "OPENAI_API_KEY": "${OPENAI_API_KEY}" }
    }
  }
}
