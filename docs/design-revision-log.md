# 설계 문서 개정 이력과 승인 기록

이 파일은 `docs/fullspec/`의 두 설계 문서(`backtest_v2_detailed_design.md`,
`backtest_v2_detailed_design_database.md`)가 언제·왜 개정되었는지와 그 개정이 승인된
사실을 남긴다. 설계 문서는 구현의 표준이므로, 개정이 조용히 일어나면 구현과 문서 중
무엇이 기준인지 알 수 없게 된다. 그래서 개정마다 사유와 승인을 여기 기록한다.

## 개정 규약

1. **설계 문서 개정은 코드 변경과 같은 커밋에 담지 않는다.** 문서 전용 커밋으로 분리한다.
   같은 커밋에 섞이면 리뷰어가 "구현이 문서를 따랐는지" 아니면 "문서가 구현을 따라
   고쳐졌는지"를 구별할 수 없다.
2. 개정 사유는 두 가지만 인정한다. 하나는 **구현이 확정한 사실을 문서에 반영**하는
   동기화이고, 다른 하나는 **설계 판단 자체를 바꾸는 개정**이다. 후자는 발주자 승인이
   필요하다.
3. 개정할 때마다 이 파일에 항목을 추가한다 — 무엇을(대상 절), 왜(사유), 어느 커밋에서,
   누가 승인했는지.

## 사후 승인 — 앞 스프린트의 문서 혼입 커밋 3건

백테스트 v2 구현 스프린트(2026-07-23~24)에서 구현 커밋 세 건이 설계 문서를 함께
개정했다. 위 규약 1을 위반한 것이며, CTO 리뷰 2가 절차 위반으로 지적하고 CTO 리뷰 3이
사후 승인 대상으로 넘겼다. 세 건의 내용은 모두 **구현이 확정한 사실을 문서에 반영한
동기화**였고 설계 판단을 바꾸지 않았으므로, 발주자 확인을 거쳐 사후 승인한다.

| 커밋 | 개정 대상 | 개정 내용과 사유 |
|---|---|---|
| `18ae360` | 데이터베이스 설계서 §5.3.1 | 결정성 해시 제외 집합에 `run_id`·`run_seq`·`run_name`과 `INTEGRITY_CHECK`의 결정성 검사 행을 추가했다. 앞의 셋은 카탈로그가 실행마다 새로 발급하거나 사람이 붙이는 값이고, 결정성 검사 행은 카탈로그의 이전 run이라는 외부 상태에 의존한다. 어느 쪽이든 같은 입력에서도 달라지므로 해시에 넣으면 결정성 검증 자체가 원리적으로 불가능해진다. 문서가 제외 집합을 벽시계·rowid·경로로만 적어 둔 것이 갭이었다. |
| `37f5c8e` | 데이터베이스 설계서 §5.2.2·§5.2.3·§5.3.3 | `config_hash` 입력 개수를 스물둘에서 스물셋으로 고치고 `resolved_indicators_json`을 추가했다. 지표 해석 결과가 해시 입력이 되었는데 문서가 그 사실을 담지 못해, 문서를 따라 만든 구현이 다른 해시를 낼 수 있었다. 아울러 무결성 검사 이름을 구현이 쓰는 표준형(`cost_once`·`deterministic`·`evidence_complete`)으로 정정했다 — 코드와 SQLite 제약과 요약 테이블 세 곳이 이미 그 이름을 쓰고 있었다. |
| `154651c` | 데이터베이스 설계서 §5.3.1 | 결정성 해시 제외 집합에 `prereg_json`을 추가했다. 사전등록 문구만 다른 run이 결정성 대조에서 거짓 실패해 평가에서 빠지던 문제를 닫은 것이다. |

**승인**: 발주자(2026-07-24, 이월 스프린트에서 확인). 세 건 모두 설계 판단 변경이 아니라
구현 사실의 문서 반영임을 확인했다. 앞으로 같은 형태의 혼입은 허용하지 않는다.

## 개정 이력

| 날짜 | 커밋 | 대상 | 사유 | 승인 |
|---|---|---|---|---|
| 2026-07-23 | `18ae360`·`37f5c8e`·`154651c` | 데이터베이스 설계서 §5.2.2·§5.3.1 | 구현 사실 반영(위 표 참조). 코드와 같은 커밋에 혼입된 절차 위반이며 사후 승인함 | 발주자 |
| 2026-07-24 | `f7af7ca` | 상세 설계서 §4.3.1·§4.3.2·§4.3.5·§4.4.1·§4.4.2·§4.5, 데이터베이스 설계서 §5.2.1~§5.2.4·§5.2.6·§5.3.1~§5.3.3·§5.3.6·§5.3.7 | 이월 항목 B와 D. 격리 마진 펀딩 회계, 진입 예약 규약, 펀딩 실측 조회 창, 결측 계약, 결정성 대조 키, Evidence 스키마 1.2.0과 `theoretical_payment_amount`, 해시 보증 범위를 구현에 맞춰 동기화. **문서 전용 커밋으로 분리해 규약을 지켰다** | 발주자 |
| 2026-07-24 | `c5091ce`·`c9d07da`·`0bb77c4`·`f1a10ea`·`093b2f5`·`9be989f` | 상세 설계서 §4.1.2·§4.4.1·§4.5·부록.5, 데이터베이스 설계서 §5.2.2·§5.2.3·§5.2.6·§5.3.1~§5.3.3 | 이월 항목 B 보충. 실행 지표 경로를 증분 방식으로 확정하고 벡터화 방식을 연구·일치 검증 기준으로 둔 설계 판단 변경, 지표 대상의 단일 레지스트리 해석, 데이터 품질 판정 입력의 별도 해시 대상 기록, 결정성 대조 상세 상태, 패키지 메타데이터 버전, 실데이터 회귀 매트릭스를 동기화했다. `f1a10ea`는 서식만 바꿔 설계 사실을 추가하지 않았음을 함께 확인했다. **문서 전용 커밋으로 분리했다** | 발주자(2026-07-24 승인) |

위 마지막 행의 유일한 설계 판단 변경인 §4.1.2 지표 실행 경로는 아래에서 발주자 승인을 받았다. 나머지는 이미
확정된 구현과 검증 체계를 문서에 맞춘 동기화이므로 별도 판단 승인이 필요 없다.

## 설계 판단 승인 — §4.1.2 지표 실행 경로를 증분 방식으로 확정

**대상.** 설계서 §4.1.2는 지표를 벡터화(전체 구간 일괄 계산)와 증분(확정 캔들마다 갱신) 두 방식으로
제공하도록 정했다. 이월 항목 E는 **실행 경로를 증분 방식으로 단독 확정**하고, 벡터화 방식은 두 경로가
동치임을 대조하는 검증 기준(파리티 오라클)으로만 남긴다. 이것이 설계 판단 변경이므로 발주자 승인을 받는다.

**승인 근거.** 이 변경은 look-ahead(미래 데이터 참조) 구조적 배제라는 협상 불가 불변식을 지키기 위한 것이다.
전체 구간을 벡터화하면 미래 캔들까지 담긴 배열에서 현재 값을 꺼내게 되어, 미래 참조를 구조적으로 막던 성질이
"조심하면 걸리지 않는다"는 규율 수준으로 내려간다. 증분 방식은 갱신 시점에 미래 캔들이 애초에 존재하지
않으므로 그 불변식을 구조로 강제한다. 두 계산 방식이 여전히 파리티 테스트에서 상대 오차 1e-12로 대조되므로,
벡터화 경로는 실행에 쓰이지 않아도 증분 구현을 검증하는 독립 기준으로서 의미를 유지한다.

**승인.** 발주자(2026-07-24). CTO 리뷰 2가 기술적 타당성을 확인하고 승인을 권고했으며, 이 판단이
look-ahead 불변식을 강화함을 발주자가 확인했다.

## 해시 보증 범위 (요약 — 정본은 데이터베이스 설계서 §5.3.1)

결정성 해시는 **실행 재현성**을 보증한다. 같은 코드·같은 원천 데이터·같은 실행 설정·같은
seed로 다시 돌리면 시뮬레이션이 만든 사실이 값 단위로 같다는 뜻이다. 여기서 "사실"은 해시
대상에 든 것을 말한다 — 제외 집합에 있는 값(벽시계·rowid·경로, `run_id`·`run_seq`·
`run_name`, 결정성 검사 행, `prereg_json`)은 인스턴스 컨텍스트나 외부 상태에 의존해 같은
입력에서도 달라지므로 애초에 재현 대상이 아니다.

해시가 보증하지 **않는** 것은 사전등록 무결성이다. `prereg_json`이 제외 집합에 있어,
판정 경로가 뒤집히지 않는 범위에서 성공·실패 기준을 바꿔도 해시는 같게 나온다. 사전등록
무결성은 `backtest_prereg`의 잠긴 사본(`locked_at`)이 단독으로 지킨다.
