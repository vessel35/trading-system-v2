[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "wallet-service"
version = "0.1.0"
description = "Paper-only signal execution, risk, and accounting driver."
requires-python = ">=3.12"
dependencies = [
    "core-lib==0.2.0",
    "psycopg[binary]>=3.2",
    "service-commons==0.1.0",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.14",
    "pytest>=8.3",
    "ruff>=0.9",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["wallet_service*"]

[tool.setuptools.package-data]
wallet_service = ["py.typed"]

[tool.uv.sources]
core-lib = { path = "../core-lib", editable = true }
service-commons = { path = "../service-commons", editable = true }

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
    "--strict-config",
    "--strict-markers",
    "-m",
    "not integration",
]
markers = [
    "integration: uses an explicitly disposable local wallet database",
]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["wallet_service", "tests"]
target-version = "py312"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]

[tool.mypy]
files = ["wallet_service", "tests"]
mypy_path = "$MYPY_CONFIG_FILE_DIR/../core-lib:$MYPY_CONFIG_FILE_DIR/../service-commons:$MYPY_CONFIG_FILE_DIR/../signal-service"
python_version = "3.12"
strict = true
warn_unreachable = true
