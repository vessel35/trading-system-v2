"""Hold money-management policies that are deployed by placing a file here.

Same rule as the strategies package: ``trading_plugins.discovery`` imports every
module beside this one and keeps the classes that inherit
``MoneyManagementBase``, which already carry their own ``id``.
"""
